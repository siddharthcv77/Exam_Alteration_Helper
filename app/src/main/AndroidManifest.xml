<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.examalterationhelper">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".register_fac" />
        <activity
            android:name=".admin_page"
            android:label="@string/title_activity_admin_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".admin_pg"
            android:label="@string/title_activity_admin_pg"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".update_details" />
        <activity android:name=".select_exam_date" />
        <activity android:name=".view_timetable" />
        <activity android:name=".ListAdapterClass" />
        <activity android:name=".update_timetable" />
        <activity android:name=".forgot_password" />
        <activity android:name=".select_date_request" />
        <activity android:name=".received_requests"></activity>
    </application>

</manifest>